# دليل اختبار القيم الحدية P2

أضيف اختبار مستقل في `src/__tests__/p2BoundaryValues.test.ts`، ويمكن تشغيله من جذر المشروع بالأمر:

```bash
npm run test:p2:boundaries
```

## ما الذي يختبره؟

| المجال | القيم المغطاة |
|---|---|
| المبالغ | `0.01` كأصغر قيمة موجبة، الصفر، السالب، `NaN`، `Infinity`، والنص الفارغ. |
| إجمالي الطلب | مبلغ كبير بكسور عشرية، دفعة مساوية للإجمالي، ومحاولة تجاوز الإجمالي. |
| المخزون | الوصول إلى صفر بالضبط، محاولة النزول تحت الصفر، كمية صفرية أو سالبة، وتكلفة وحدة صفرية. |
| WAC | إدخال كمية صغيرة جدًا عند رصيد صفر، وإخراج كامل المخزون حتى يصبح الرصيد صفرًا. |
| الدفعات | دفعة تساوي المتبقي، دفعة أكبر من المتبقي، حساب الزيادة، وقيم دفع غير صالحة. |
| سجل الدفعات | الوصول إلى متبقي صفر، ومنع تكرار معرف الدفعة. |
| النصوص | ملاحظة تتجاوز 2000 حرف مع مبلغ كبير للتأكد من عدم القص أو التحويل الرقمي غير المقصود. |

## التشغيل السريع

للفحص المركز أثناء التطوير:

```bash
npm run test:p2:boundaries
```

للفحص الكامل بعد تعديل القواعد أو الواجهات:

```bash
npm run quality && npm run test && npm run build
```

ولفحص الطباعة بعد القيم الطويلة:

```bash
npm run test:print:desktop
```

> نجاح الاختبار يعني أن قواعد المجال الحالية تعاملت مع الحالات الحدية كما هو متوقع. إذا فشل اختبار، لا يُنصح بتعديل التوقع مباشرة؛ يجب أولًا تحديد هل تغيّر العقد المقصود أم ظهرت مشكلة فعلية في الحساب أو التحقق.

## نتيجة التشغيل الحالي

آخر تشغيل: **8 اختبارات ناجحة من أصل 8**. الاختبار معزول عن قاعدة بيانات المستخدم ولا ينشئ سجلات تشغيلية حقيقية.
