# تقرير تحسين أداء وذاكرة Electron — صهوة

## التحسينات المطبقة

تم فصل مكتبة `xlsx` من حزمة شاشة التقارير وتحميلها عند الضغط على تصدير Excel فقط. انخفض حجم ملف شاشة التقارير من نحو 305KB إلى نحو 22KB، بينما أصبحت مكتبة Excel في chunk مستقل لا يُحمّل مع التشغيل الأول للشاشة.

تم ضغط شعار SVG باستخدام SVGO مع الاحتفاظ بنسخة احتياطية. انخفض حجم الشعار من 201,144 بايت إلى نحو 84,620 بايت قبل البناء، وانخفضت نسخة الشعار النهائية في الحزمة من نحو 196KB إلى نحو 83KB خامًا.

تم تحسين بداية تحميل التطبيق باستخدام `Promise.all` لجلب البيانات والتفضيلات بالتوازي، وإضافة قفل يمنع تنفيذ طلبات AppData متزامنة، وتنظيف مؤقت التراجع عند تفكيك App. هذه التغييرات تقلل الانتظار وإعادة الطلبات غير الضرورية دون تغيير البيانات.

تم تحسين SQLite بإضافة `busy_timeout`، و`synchronous=NORMAL` ضمن WAL، و`temp_store=MEMORY`، وتنفيذ `PRAGMA optimize` بعد الترحيل. كما أضيفت migration رقم 005 بفَهارس للبحث في العملاء، وفرز الطلبات حسب الحالة والتسليم، والفواتير، والتنبيهات، وحركات المخزون، ومعاملات الصندوق.

تم جعل إغلاق قاعدة البيانات idempotent مع إيقاف مؤقت النسخ الدوري، كما أصبح مسار خروج Electron يمنع تكرار النسخ الاحتياطي والإغلاق عند تعاقب `window-all-closed` و`before-quit`.

تم تحديث إعدادات حزمة Electron لاستبعاد Source Maps من التوزيع النهائي، مع إبقائها في مجلد البناء المحلي عند الحاجة للتشخيص. وأضيف أمر `perf:report` لإنتاج تقرير قابل لإعادة التشغيل لأحجام الحزم الخام والمضغوطة.

## القياس

| المؤشر | قبل التحسينات الأخيرة | بعد التحسينات الأخيرة |
|---|---:|---:|
| إجمالي أصول الواجهة خامًا | نحو 1,198.6KB | نحو 1,085.0KB |
| إجمالي أصول الواجهة Gzip | نحو 363.5KB | نحو 329.3KB |
| ملف شاشة التقارير | نحو 305KB | نحو 21.7KB |
| شعار SVG النهائي | نحو 196KB | نحو 82.6KB |
| مكتبة xlsx | ضمن شاشة التقارير | chunk مستقل، نحو 429.5KB ولا تُحمّل إلا عند التصدير |

## الاختبارات النهائية

نجح فحص TypeScript والبناء الإنتاجي وبناء Electron. نجحت الاختبارات الآلية، كما نجح اختبار التكامل الكامل الذي يغطي جسر IPC الحقيقي، قاعدة البيانات، الطلبات، الفواتير، المخزون، التقارير، النسخ الاحتياطي، الاستعادة، التراجع الذري، والإغلاق وإعادة الفتح.

تم تشغيل الواجهة عند 1440×1000، وظهرت لوحة التحكم كاملة دون شاشة بيضاء أو أخطاء JavaScript غير معالجة. رسائل أخطاء SQLite الظاهرة أثناء الاختبار مقصودة ضمن سيناريوهات اختبار التحقق والتراجع، بينما كانت جميع الحالات النهائية ناجحة.

## ملاحظات الإصدار

النسخة مناسبة للانتقال إلى اختبار قبول Electron النهائي. يوصى قبل النشر الفعلي بإجراء اختبار يدوي على جهاز Windows مستهدف مع بيانات حقيقية منسوخة احتياطيًا، وقياس الذاكرة بعد جلسة استخدام طويلة، وتجربة تصدير Excel مرة واحدة للتأكد من تحميل chunk الخاص بها عند الطلب.
