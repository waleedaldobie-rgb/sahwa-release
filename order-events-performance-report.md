# تقرير اختبار أداء SQLite وسجل أحداث الطلب

## نطاق الاختبار

أُجري اختبار ضغط معزول على SQLite باستخدام بيئة Electron نفسها التي يستخدمها التطبيق، مع تفعيل `WAL` و`foreign_keys` و` synchronous=NORMAL`. لم يتم استخدام قاعدة بيانات العميل أو أي ملف بيانات حقيقي؛ أُنشئت ملفات مؤقتة وحُذفت بعد كل جولة.

اختبر السيناريو إنشاء طلبات مع ثلاثة أحداث لكل طلب، ثم قراءة سجل طلب واحد بترتيب زمني، وقراءة أحدث 100 حدث على مستوى النظام. شملت الأحجام 1,000 و10,000 و50,000 طلب، أي حتى 150,000 حدث.

## الملاحظة الأولية والإصلاح

كان استعلام أحدث الأحداث في handler يستخدم:

```sql
SELECT * FROM order_events ORDER BY created_at DESC;
```

ومع وجود فهرس `(order_id, created_at)` كان استعلام الطلب الواحد سريعًا، لكن الاستعلام العام بدأ يزداد خطيًا مع عدد الأحداث. لذلك أُضيف فهرس مستقل:

```sql
CREATE INDEX IF NOT EXISTS idx_order_events_created_at
ON order_events(created_at);
```

هذا التعديل منخفض المخاطر؛ فهو يُنفذ تلقائيًا عبر `CREATE INDEX IF NOT EXISTS` عند تشغيل قاعدة البيانات الحالية، ولا يغير بنية البيانات أو منطق الطلبات.

## النتائج بعد الإصلاح

| عدد الطلبات | عدد الأحداث | زمن الإدخال | سرعة الإدخال | قراءة سجل طلب واحد: المتوسط | قراءة سجل طلب واحد: P95 | قراءة أحدث 100 حدث: المتوسط | قراءة أحدث 100 حدث: P95 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 3,000 | 24.29 ms | 41,167 طلب/ثانية | 0.0088 ms | 0.0102 ms | 0.1288 ms | 0.1621 ms |
| 10,000 | 30,000 | 241.92 ms | 41,335 طلب/ثانية | 0.0075 ms | 0.0094 ms | 0.1151 ms | 0.1450 ms |
| 50,000 | 150,000 | 1,247.67 ms | 40,075 طلب/ثانية | 0.0083 ms | 0.0133 ms | 0.1213 ms | 0.1321 ms |

## المقارنة قبل إضافة الفهرس

| عدد الأحداث | قراءة أحدث 100 حدث قبل الفهرس | قراءة أحدث 100 حدث بعد الفهرس | التحسن التقريبي |
|---:|---:|---:|---:|
| 3,000 | 2.7601 ms | 0.1288 ms | 95.3% |
| 30,000 | 29.0671 ms | 0.1151 ms | 99.6% |
| 150,000 | 159.5368 ms | 0.1213 ms | 99.9% |

## التحقق من خطة الاستعلام وسلامة البيانات

أكد `EXPLAIN QUERY PLAN` استخدام الفهرس `idx_order_events_order_date (order_id=?)` عند قراءة سجل طلب محدد، واستخدام الفهرس `idx_order_events_created_at` عند قراءة أحدث الأحداث العامة. كما أعاد `PRAGMA integrity_check` القيمة `ok` في جميع الأحجام، وأعاد `PRAGMA foreign_key_check` نتيجة فارغة، مما يعني عدم وجود مخالفات في العلاقات الأجنبية أثناء الاختبار.

| الفحص | النتيجة |
|---|---|
| سلامة SQLite | `ok` في جميع الجولات |
| العلاقات الأجنبية | لا توجد مخالفات |
| فهرس سجل الطلب الواحد | مستخدم فعليًا |
| فهرس أحدث الأحداث | مستخدم فعليًا |
| تكرار الأحداث في الاختبار | لا يوجد؛ لكل حدث معرف فريد |

## النتيجة والتوصية

أداء سجل الأحداث مناسب جدًا للاستخدام اليومي، وحتى مع 50,000 طلب و150,000 حدث بقيت قراءة سجل طلب واحد أقل من `0.02 ms` تقريبًا عند P95، وبقيت قراءة أحدث 100 حدث قريبة من `0.15 ms` عند P95. الفهرس الجديد عالج نقطة التباطؤ الوحيدة التي ظهرت في الاختبار الأول.

تمت إعادة تشغيل اختبارات المشروع بعد التعديل، وكانت النتيجة **14/14 اختبارًا ناجحًا**، مع نجاح TypeScript strict وVite build و`git diff --check`.

## أداة إعادة الاختبار

تم حفظ benchmark قابل لإعادة التشغيل في:

```text
scripts/order-events-performance.cjs
```

ويُشغّل عبر Electron runtime حتى يستخدم نفس ABI الخاص بـ `better-sqlite3` الموجود في تطبيق سطح المكتب. ملفات قاعدة الاختبار مؤقتة ولا تمس قاعدة بيانات العميل.
