# Sahwa Production — تقرير تدقيق UI/UX النهائي بعد UI Polish

## نطاق الجولة

تمت مراجعة النسخة الحالية من مشروع Sahwa وتطبيق جولة UI Polish مركزة على واجهة سطح المكتب العربية RTL، مع الحفاظ على الهوية البصرية القائمة: **Charcoal / Black، Brass، Off-white، Tajawal، RTL، وDesktop-first**. ركزت التعديلات على وضوح التسلسل التشغيلي، تقليل كثافة الإجراءات، توحيد عناصر التحكم، الوصولية، القراءة المختلطة للأرقام العربية واللاتينية، والحالات الفارغة والتحميل والخطأ.

تم الحفاظ على منطق الأعمال وعقود IPC وقاعدة البيانات والحسابات المالية ودورة حالات الطلبات. التعديل الوحيد خارج طبقة العرض كان إصلاحًا معزولًا في `electronMock` لضمان عدم فقدان سجلات WhatsApp الأحدث عند حفظ snapshot قديم أثناء الاختبارات. أضيفت كذلك قناة PDF محصورة في وضع UI automation لاختبار الطباعة آليًا، بينما بقي مسار الطباعة الإنتاجي الحالي قائمًا على `window.print`.

## Findings and Implemented Fixes

| المعرّف | الشاشة / النطاق | الأولوية | المشكلة | الإصلاح المنفذ | الحالة |
|---|---|---:|---|---|---|
| UX-001 | المخزون | HIGH | غياب البحث والتصفية السريعة لحالة المخزون، وعدم التمييز النصي الكافي لحالة النفاد. | بحث عربي، عداد نتائج، فلتر «الكل / متوفر / منخفض / نفد»، وشارات نصية واضحة. | FIXED |
| UX-002 | المخزون | MEDIUM | عدم ظهور سعر الشراء وبعض حقول حد التنبيه في الجداول والنماذج. | إضافة أسعار الشراء وحدود التنبيه والكمية والوحدة إلى مناطق القرار التشغيلي، مع حفظ أسعار الإكسسوارات بشكل صحيح. | FIXED |
| UX-003 | الطلبات وتفاصيل الطلب | HIGH | هوية العميل ورقم الطلب وموعد التسليم والملخص المالي لم تكن في أعلى وضوح تشغيلي. | تحسين بطاقة الهوية والملخص المالي وإظهار الإجمالي والمدفوع والمتبقي وموعد التسليم بترتيب أوضح. | FIXED |
| UX-004 | الطلبات | HIGH | كثافة أزرار WhatsApp والطباعة داخل كل صف. | إبقاء الإجراءات الأساسية ظاهرة وتجميع الإجراءات الثانوية في `details.sahwa-actions-menu` أصلي قابل للفتح بلوحة المفاتيح، مع Tooltips وسمات وصول. | FIXED |
| UX-005 | الطلبات والفواتير والتقارير | MEDIUM | اختلاف تصميم الفلاتر بين الشاشات. | إضافة `SegmentedControl` مشترك بحالة `aria-pressed`، تركيز موحد، ارتفاعات موحدة، وتسميات عربية. استُخدم في Orders وInventory وInvoices وReports وAccounting. | FIXED |
| UX-006 | الحالات الفارغة | MEDIUM | بعض الحالات لا تميز بوضوح بين عدم وجود بيانات وعدم وجود نتائج بعد تطبيق فلتر. | رسائل مستقلة للحالتين مع زر «مسح البحث والفلاتر» عند الحاجة، خصوصًا في Orders وInventory وInvoices. | FIXED |
| UX-007 | الفواتير والتحصيل | HIGH | ملخص التحصيل كان يعرض المتبقي فقط ولا يوضح أثر الدفعة الجديدة. | ملخص ثلاثي للإجمالي والمدفوع سابقًا والمتبقي بعد الدفعة، مع حد أقصى واضح للتحصيل وتحديث حي. | FIXED |
| UX-008 | المحاسبة والتقارير | MEDIUM | طرق الدفع والمصطلحات الداخلية كانت أقل وضوحًا أو غير عربية بالكامل. | شارات دلالية لطرق الدفع، وتعريب المصطلحات الظاهرة دون تغيير مفاتيح التصدير أو الحسابات. | FIXED |
| UX-009 | RTL والأرقام | MEDIUM | القراءة المختلطة لأرقام الطلبات والجوال والتواريخ والمبالغ تحتاج عزلًا دلاليًا. | اتجاه RTL صريح على مستوى التطبيق، وقواعد `direction: ltr` و`unicode-bidi: isolate` وtabular numerals للحقول الرقمية والجداول. | FIXED |
| UX-010 | الوصولية وMicro UX | MEDIUM | الحاجة إلى اتساق أكبر في التركيز، الإغلاق، وحالات التحديث. | `type="button"` افتراضي، `aria-busy` أثناء تحديث لوحة التحكم، `aria-pressed` للفلاتر، Focus-visible موحد، ودعم الحركة المخفضة. | FIXED |
| UX-011 | التحقق البصري | HIGH | كان فحص الأحجام لا يغطي المصفوفة الموصى بها ولم يكن fixture واقعيًا أو ذاتي التنظيف. | توسيع الفحص إلى 1280×800 و1440×900 و1440×1000 و1920×1080، مع بيانات اختبارية تشمل المخزون والفواتير والمشتريات والمصروفات، ثم استعادة البيانات الأصلية تلقائيًا. | FIXED |
| UX-012 | تفاصيل الطلب والوصولية | HIGH | لم يكن مسار فتح التفاصيل والإغلاق بـ Escape وإعادة التركيز مغطى باختبار قبول مستقل. | إضافة اختبار قبول يفتح التفاصيل، يتحقق من الحوار والبيانات الطويلة، ثم يغلق بـ Escape ويتحقق من عودة التركيز إلى زر «عرض». | FIXED |
| UX-013 | معاينة الفاتورة والطباعة | HIGH | كانت نسخة الطباعة المكررة تنتج صفحات زائدة، كما لم يكن اختبار المحتوى الطويل وPDF قابلًا لإعادة التشغيل. | منع النسخة المخفية من الطباعة أثناء المعاينة، تثبيت modal print host، إضافة قناة PDF آلية، وفحص ملاحظات متعددة الأسطر ومبلغ كبير. | FIXED |

## ما تم الحفاظ عليه

بقيت عناصر كثيرة دون إعادة تصميم لأنها كانت ناضجة ومتوافقة مع الهوية الحالية، ومنها نظام التوكنز، خط Tajawal، إدارة التركيز داخل Modal، حماية الإرسال المكرر، شارات حالات الطلبات، الفرز الدلالي للجداول، حالات Empty State، Sidebar وHeader، الطباعة الحالية، وإعدادات المحل.

لم تتم إضافة Mobile layout أو Dark mode أو Breadcrumbs أو Virtualization أو Charts زخرفية أو Architecture جديدة؛ لأن ذلك خارج نطاق UI Polish المكتبي الحالي.

## الملفات المعدلة

| الملف | نطاق التعديل |
|---|---|
| `src/components/ui.tsx` | Button آمن افتراضيًا، وإضافة `SegmentedControl` المشترك. |
| `src/components/OrdersView.tsx` | تجميع الإجراءات الثانوية، RTL صريح، فلتر مشترك، وحالة فارغة موجهة. |
| `src/components/InventoryView.tsx` | فلتر مشترك، حالات فارغة قابلة للمسح، أسعار وتكلفة وحدود تنبيه. |
| `src/components/InvoicesView.tsx` | فلتر مشترك وملخص تحصيل حي للإجمالي والمدفوع والمتبقي. |
| `src/components/AccountingView.tsx` | توحيد تبويبات المحاسبة مع التحكم المشترك. |
| `src/components/ReportsView.tsx` | توحيد فلتر الفترة الزمنية وتعريب المصطلحات الظاهرة. |
| `src/components/App.tsx` | RTL دلالي على الجذر و`aria-busy` أثناء تحديث لوحة التحكم. |
| `src/styles/tokens.css` | أنماط SegmentedControl، disclosure actions، عزل الأرقام، Focus، والحركة المخفضة. |
| `src/services/adapters/inventoryCatalogDraftAdapter.ts` | حفظ أسعار شراء وبيع الإكسسوارات عند الإنشاء. |
| `scripts/desktop-visual-viewport-check.mjs` | fixture واقعي ذاتي التنظيف ومصفوفة أحجام كاملة. |
| `src/__tests__/segmentedControlUi.test.tsx` | اختبارات جديدة لحالة الفلتر وسمات الوصول. |
| `src/__tests__/customerCreditUi.test.tsx` | تحديث توقعات فلتر التقارير المشترك. |
| `src/__tests__/tableSortingUi.test.tsx` | تحديث توقعات قائمة الإجراءات الثانوية الأصلية. |
| `src/__tests__/orderDetailsAcceptanceUi.test.tsx` | اختبار قبول فتح تفاصيل الطلب وEscape وإعادة التركيز. |
| `src/electron/main.ts` | قناة PDF محصورة في وضع UI automation باستخدام `webContents.printToPDF`. |
| `src/electron/preload.ts` و`src/types.ts` | تعريف جسر PDF الاختباري بشكل آمن واختياري. |
| `package.json` | إضافة الأمرين `npm run test:print:desktop` و`npm run test:p2:boundaries`. |
| `src/__tests__/p2BoundaryValues.test.ts` | تغطية القيم الحدية للمبالغ والمخزون والدفعات والنصوص الطويلة. |
| `P2-BOUNDARY-TEST-GUIDE.md` | خطوات التشغيل السريع ومصفوفة القيم المغطاة. |

## نتائج التحقق البصري

تم تشغيل 16 حالة قبول: ثلاث حالات واجهة (`populated`, `loading`, `error`) وحالة طباعة (`print`) × أربعة أحجام سطح مكتب. لم يظهر overflow أفقي في أي حالة، ونجحت إشارات التحميل والخطأ، ونجحت الحالة المعبأة مع وجود جداول وبطاقات وبيانات اختبارية. أنتجت حالة الطباعة PDF واحد الصفحة لكل حجم؛ الوسيط الفعلي A4 حسب CSS الحالي، مع جسم فاتورة بتخطيط 15×21 سم داخله.

| الحالة | الأحجام | النجاح |
|---|---|---:|
| `populated` | 1280×800، 1440×900، 1440×1000، 1920×1080 | 4/4 |
| `loading` | 1280×800، 1440×900، 1440×1000، 1920×1080 | 4/4 |
| `error` | 1280×800، 1440×900، 1440×1000، 1920×1080 | 4/4 |
| `print` | 1280×800، 1440×900، 1440×1000، 1920×1080 | 4/4 — PDF من صفحة واحدة لكل حجم، مع وجود المبلغ الطويل. |
| **الإجمالي البصري** | **16 حالة** | **16/16** |

## التحقق البرمجي النهائي

| الأمر | النتيجة |
|---|---|
| `npm run quality` | **PASS** — typecheck للنطاق والمحوّلات وTypeScript lint. |
| `npm run test` | **PASS** — 28 ملف اختبار و142 اختبارًا ناجحًا. رسائل rollback المتوقعة في سيناريوهات الرفض لا تمثل فشلًا. |
| `npm run test:p2:boundaries` | **PASS** — 8/8 اختبارات للقيم الحدية. |
| `npm run build` | **PASS** — إنشاء renderer production bundle. |
| `npm run build:electron` | **PASS** — إنشاء ملفات Electron في `dist-electron/`. |
| `npm run perf:report` | **PASS** — إجمالي الحزمة 789.9 KB خام و220.8 KB gzip وفق التقرير. |
| `npm run test:visual:desktop` | **PASS** — 12/12 حالة واجهة، بلا overflow أفقي. |
| `npm run test:print:desktop` | **PASS** — 4/4 ملفات PDF، كل ملف صفحة واحدة، ووجود الرقم `987654.32` داخل النص المستخرج. |
| اختبار قبول تفاصيل الطلب | **PASS** — فتح الحوار، ظهور البيانات الطويلة، Escape، وإعادة التركيز إلى زر «عرض». |

## قبول مسار تفاصيل الطلب والطباعة

تمت إضافة تغطية قبول لمسار تفاصيل الطلب. يفتح الاختبار طلبًا باسم ورقم طويلين، يتحقق من ظهور الحوار والملخص المالي، ثم يرسل Escape ويتحقق من اختفاء الحوار وعودة التركيز إلى زر «عرض». يعتمد الاختبار على جسر أحداث معزول ولا يغير بيانات الإنتاج.

كما أضيف اختبار P2 مستقل لاختبار الصفر والسالب و`NaN` و`Infinity`، الدفعة المساوية للمتبقي والدفعة الزائدة، الوصول إلى صفر مخزون، تكلفة الوحدة الصفرية، WAC، معرفات الدفعات المكررة، والملاحظات التي تتجاوز 2000 حرف. تفاصيل التشغيل موجودة في `P2-BOUNDARY-TEST-GUIDE.md`.

في مسار الطباعة، كشفت المعاينة الأولية عن نسخة فاتورة مكررة وصفحة فارغة تقريبًا. تم إصلاح ذلك بمنع `hidden-on-screen` من الظهور عند فتح المعاينة وتثبيت `modal-print-host` في أصل صفحة الطباعة. أصبح `npm run test:print:desktop` قابلًا لإعادة التشغيل، وينتج PDF من صفحة واحدة لكل حجم مع التفاف الملاحظات الطويلة وظهور المبلغ الكبير دون قص أو تكرار. يستخدم الاختبار A4 كوسيط فعلي وفق CSS الحالي، ويضع جسم الفاتورة بتخطيط 15×21 سم داخله.

## إصلاح دورة WhatsApp

كانت الإخفاقات الأربعة السابقة ناتجة عن مسار حفظ mock: إشعار WhatsApp الجديد أو الأحدث كان يضيع عندما يحفظ renderer snapshot قديمًا يحتوي على إشعارات المخزون فقط. تم إصلاح الدمج حسب `source + sourceId` مع أولوية `updatedAt` الأحدث، فبقيت حالات `pending` و`sent` و`failed` و`retry` وسجل المحاولات محفوظة دون السماح لـ stale snapshot بالكتابة فوق سجل أحدث. التفاصيل الكاملة موجودة في `WHATSAPP-TEST-FIX-REPORT.md`.

## حماية منطق الأعمال

لم تتغير Accounting أو Payments أو Inventory calculations أو Material Usage أو Order State Machine أو Reports Calculations أو Database Schema أو IPC Contracts أو Backup/Restore. تعديل `electronMock` محصور بدمج بيانات الاختبارات، وتعديل adapter الإكسسوارات يحفظ حقولًا موجودة أصلًا في العقد دون تغيير طريقة الحساب.

## الحكم النهائي

**UI POLISH APPLIED — P1 ACCEPTANCE PASSED — READY FOR PRODUCT QA**. تم تطبيق تحسينات تفاصيل الطلب والطباعة ذات الأولوية، وأصبح مسار Escape وإعادة التركيز مغطى آليًا، كما اجتازت الطباعة ببيانات طويلة اختبار PDF من صفحة واحدة عبر الأحجام الأربعة. تبقى فقط جولة بشرية اختيارية على الطابعة المستهدفة للتأكد من إعدادات الورق والحبر الفعلية قبل الإصدار الإنتاجي.
