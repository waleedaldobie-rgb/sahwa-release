
## نتيجة المعاينة الأولية

ملف `print-1280x800.pdf` بحجم 15×21 سم أظهر الفاتورة الرئيسية في الصفحة الأولى، مع ظهور الملاحظات الطويلة والتفافها دون قص واضح. ظهرت صفحة ثانية فارغة تقريبًا، ثم تكررت الفاتورة في الصفحة الثالثة. هذا يعني أن مسار الطباعة الحالي لا يزال يطبع نسخة إضافية من واجهة المعاينة أو عنصرًا مخفيًا، ويجب تقييده إلى print host واحد قبل اعتماد P1.

## الإصلاح والنتيجة النهائية

تمت معالجة السبب في `InvoicesView` بمنع نسخة `hidden-on-screen` من الظهور عند فتح معاينة الفاتورة، ثم تثبيت موضع `modal-print-host` أعلى صفحة الطباعة. أضيف مسار PDF آمن ومحصور في وضع UI automation عبر `webContents.printToPDF`، وأضيف الأمر `npm run test:print:desktop`.

بعد الإصلاح، نجح اختبار الطباعة في الأحجام الأربعة، وأنتج كل حجم ملف PDF من صفحة واحدة. اعتمد Electron ورقة A4 لأن CSS الحالي يعرّف A4 كوسيط الطباعة الفعلي ويضع جسم الفاتورة بتخطيط 15×21 سم داخله. ظهرت المبالغ الكبيرة والملاحظات متعددة الأسطر داخل PDF دون قص بصري أو تكرار، وتأكد `pdftotext` من وجود الرقم `987654.32`.

كما أضيف اختبار قبول آلي لمسار تفاصيل الطلب في `orderDetailsAcceptanceUi.test.tsx`: يفتح زر «عرض» التفاصيل، يتحقق من ظهور الحوار والبيانات الطويلة، ثم يغلقه بـ Escape ويتحقق من عودة التركيز إلى زر «عرض».
